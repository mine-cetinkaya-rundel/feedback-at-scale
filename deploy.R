rsconnect::deployApp(
  appDir = "learnr",
  appName = "feedback-at-scale",
  appTitle = "Feedback at scale",
  account = "minecr",
  forceUpdate = TRUE,
  launch.browser = TRUE
)

# instance size - largest possible
# max worker - 2
# max connections - 20
# start count - 25